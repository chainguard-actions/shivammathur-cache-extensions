name: Test basic usage with php-version
on:
  workflow_dispatch:
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
      - id: action
        uses: .
        with:
          php-version: '8.2'
          extensions: 'mbstring, intl'
          key: 'test-key-v1'
      - name: Assert dir output is set
        env:
          DIR: ${{ steps.action.outputs.dir }}
        run: |
          echo "dir: $DIR"
          if [ -z "$DIR" ]; then
            echo "FAIL: expected dir output to be non-empty"
            exit 1
          fi
          echo "PASS"
      - name: Assert key output is set
        env:
          KEY: ${{ steps.action.outputs.key }}
        run: |
          echo "key: $KEY"
          if [ -z "$KEY" ]; then
            echo "FAIL: expected key output to be non-empty"
            exit 1
          fi
          echo "PASS"
